# Rust / WASM build artifacts
/engine/target/
/engine/pkg/
/engine/wasm-pack.log
/frontend/src/wasm/

# JavaScript / Node.js
/frontend/node_modules/
/frontend/dist/

# Generated test reports. Regenerated by `npm run test:coverage`; what the project
# report keeps is a summary in documentation/, not the HTML output itself.
/frontend/coverage/

# Machine-local Claude Code settings (absolute paths, per-session scratchpads).
# The shared, committed allowlist lives in .claude/settings.json.
/.claude/settings.local.json

# Environment files
.env
.env.local
.env.*.local

# OS artefacts
.DS_Store
Thumbs.db
